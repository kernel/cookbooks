# Anthropic API key — drives Managed Agents (vaults, agents, sessions). Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Kernel API key — stored in a vault and injected into the agent sandbox as an opaque placeholder. Get one at https://dashboard.onkernel.com/
KERNEL_API_KEY=
